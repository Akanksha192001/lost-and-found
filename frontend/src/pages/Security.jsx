import React from "react";

export default function Security() {
  return (
    <div className="card">
      <div className="header">
        <h2 className="pageTitle">Security Hand-off</h2>
      </div>
      <p>
        Security staff will confirm item returns in this workspace. Access it with a security role
        account to verify the new authentication flow.
      </p>
    </div>
  );
}
